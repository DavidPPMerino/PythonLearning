""" Listas """
# As listas são criadas da mesma forma que as tuplas mas utilizam [] em vez de ().
# Ao contrario das tuplas é possivel adicionar ou editar elementos.

# lanche = [hamburguer, sumo, pizza, pudim]
# lanche[3] = gelado # muda o pudim pelo gelado

# lanche.append('cookie') # adiciona uma cookie ao final da lista
# lanche.insert(0, 'hotdog') # adiciona um hotdog na posição 5 movendo a lista toda

# del lanche[3] # apaga o elemento 3 da lista
# lanche.pop(3) # apaga o elemento 3 da lista mas habitualmente é usado para apagar o ultimo
# lanche.remove('pizza') # apaga o elemento pizza através do nome


# valores = list(range(4, 11)) # lista criada atraves de range
# valores.sort() # ordena a lista
# valores.sort(reverse=True) # ordena a lista invertida
# len(valores) # contagem real dos elementos da lista